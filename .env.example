# Required for the AI judge script (npm run judge) AND the public chat endpoint (/api/chat).
# On Vercel: set this in Project Settings → Environment Variables. NEVER commit a real key.
OPENAI_API_KEY=sk-replace-me
